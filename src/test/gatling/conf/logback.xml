<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration>

<configuration>

    <appender name="CONSOLE" class="com.icthh.xm.commons.logging.aop.SecurityMaskingConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%-5level] [${springAppName:-}] [%X{traceId:-}]  %logger{15} - %msg%n%rEx</pattern>
            <immediateFlush>false</immediateFlush>
        </encoder>

        <keywords>${LOGBACK_SECURITY_MASKING_KEYWORDS: }</keywords>
        <replacementMessage>${LOGBACK_SECURITY_MASKING_REPLACEMENT_MESSAGE:this log was removed due to potential security breach}</replacementMessage>

    </appender>

    <!-- Uncomment for logging ALL HTTP request and responses -->
    <!--    <logger name="io.gatling.http.ahc" level="TRACE" /> -->
    <!--    <logger name="io.gatling.http.response" level="TRACE" /> -->
    <!-- Uncomment for logging ONLY FAILED HTTP request and responses -->
    <!--    <logger name="io.gatling.http.ahc" level="DEBUG" /> -->
    <!--    <logger name="io.gatling.http.response" level="DEBUG" /> -->

    <root level="WARN">
        <appender-ref ref="CONSOLE" />
    </root>

</configuration>

